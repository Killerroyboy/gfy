# GFY — Backlog

> S13 format. Status: `open`|`decision`|`verify`|`blocked`|`partial`|`done`. Owner: **YOURS** (agent) | **RILEY**.
> Pipe-table with numbered bold-title rows — parses in BOTH mc-ground's needs-you grep and mc-unified's narrative panel.

| # | Item | Status | Owner | Notes |
|---|------|--------|-------|-------|
| 1 | **Pre-links runway (README "Live scoring")** | `blocked` | RILEY | Re-paste BOTH .gs on the live sheet + redeploy endpoint (Edit → New version), Info keys score_endpoint/form_url, run polish(), then the 25-min SC-DRILL (now incl. the third-number keep-then-reopen step). Captain links DO NOT go out before this passes. |
| 2 | **Real-phone scorer eyeball at 390w under production Jost** | `verify` | RILEY | S11 harness ran fallback Futura; one live phone look at the card/pad closes the font caveat (D1 fix measured, not eyeballed on-device). |
| 3 | **Sheet doc-id sits in public git history since f261fd2** | `decision` | RILEY | Recommendation: accept-residual (sheet 401s anonymous; drill re-verifies sharing). Rotate the sheet if declined. |
| 4 | **§23 copy-integrity bundle** | `open` | YOURS | In flight on branch copy-bundle (empty-state split, ownerless-collected flag, supersession note, README trio, X40 byte-assert; 213→215). |
| 5 | **Accepted follow-ups (post-tournament fine)** | `open` | YOURS | aria-modal focus/keyboard wave; board-btn confirm-handler refresh; season-pin + hole-panel-head test hardening; suppressed-board on-surface explainer. |
| 6 | **Live sheet still carries template sample data** | `open` | RILEY | Audit 2026-08-15: field/scores/calcutta/rooms are template sample rows (Duck/Hammer/Tex; sample cards edited in place) behind real-looking dates; schedule ends at Round One; no per-round tee times. Real event = next year (Riley ruling). Replace with real data before links go out — §24's event-ready.mjs will police the verbatim residue mechanically. |
| 7 | **§24 player-information wave (spec rev 2, pressure-tested)** | `open` | YOURS | Announcements tab + unseen banner, event-phase Now/Next Home (reuses existing 60s loop + visibility seam), C-FALLBACK honest fallbacks, tools/event-ready.mjs preflight (9 checks incl. cross-tab + fallback parity). 4-lens pressure test 2026-08-15: ~30 findings triaged, 2 rev-1 defects were mine (phantom 3-min poller vs existing 60s loop; date-only parseDate vs watermark). **MERGED LOCAL 2026-08-18 (v2.1-invites @ f517556, 239/239)** — 7 tasks + per-task reviews + whole-branch review (fable) + 2-item final fix wave + 18-PNG render close. Awaiting RILEY push. Post-push: create `announce` tab in live sheet (re-run polish()) + paste its gid into config.js GID.announce; then `npm run event-ready`. |
| 8 | **Offered, Riley to rule (from pressure-test scout)** | `decision` | RILEY | (a) printable one-pager/QR lodge poster (M, pull-only friendly); (b) post-event results export + Champions backfill helper (L, post-event); (c) ?preflight=1 in-site readiness view (M, operator phone-side). None folded into §24. |
| 9 | **Wire the accept→Field + invites workflow on the live sheet** | `open` | RILEY | Built 2026-08-24, two waves (247/247): (1) Invites `committed` checkbox + GFY menu "Promote committed → Field" (NOCLOBBER, since carried, team blank into draft pool); (2) Riley rulings folded in — "Seed next season's Invites" menu (returning players from trailing-3 Field seasons, veterans-first, out-for-good skipped), `invited_by` sponsor column (who invited who; admin-only "via X" on site; event-ready WARNs on sponsorless first-timers + unpromoted ticks), Invites orange/red tints, Field count strip + veterans-first pre-draft sort. Live steps after push: add `committed` + `invited_by` headers on Invites row 1 → paste gfy-promote.gs (File → New → Script file, never over Code.gs) → re-paste sheet-polish.gs (checkbox list + Invites CF + START HERE grew) → run polish() → reload sheet, GFY menu appears. README "The invite list" has the full workflow. |
