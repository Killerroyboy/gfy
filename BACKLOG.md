# GFY — Backlog

> S13 format. Status: `open`|`decision`|`verify`|`blocked`|`partial`|`done`. Owner: **YOURS** (agent) | **RILEY**.
> Pipe-table with numbered bold-title rows — parses in BOTH mc-ground's needs-you grep and mc-unified's narrative panel.
> Numbering repaired 2026-09-24: #14/#16/#17/#18 had each been issued twice by concurrent sessions; the later duplicates moved to #20-#23. No row was dropped — verify against git history before assuming one was.

## Riley's next three (CEO-ranked 2026-09-24 — cheap gates first, long pole last)

| # | Item | Status | Owner | Notes |
|---|------|--------|-------|-------|
| R1 | **Push word — unblocks everything downstream** | `decision` | RILEY | 18 commits on `v2.1-invites`, ancestry FF-verified, suite 337/337. Ships §26 event kit + the three go-live fixes below. **Order matters: the sheet-side scripts Riley pastes must come FROM pushed source**, so this one word gates the entire paste chain. Rollback ref `dd803eb`. |
| R2 | **Arm the scorer** | `blocked` | RILEY | Deploy the real handler onto the SAME deployment URL (Manage deployments → Edit → New version), paste `Info!score_endpoint`, then `npm run check-endpoint -- <url>` must print `REAL` and the drill must be run. Does NOT wait on data — per §27 this happens BEFORE the fill, so the scariest component is proven while Riley is still assembling the event. **Live probe 2026-09-24 says `STUB`: the deployed Web App is still the §18 echo and writes nothing.** |
| R3 | **The 2027 data package (one sitting, not ten pings)** | `decision` | RILEY | The long pole and the only thing only he holds — see #6 for the full decision sheet: real dates/`first_tee`/day-3 schedule row, Champions 2026 row (bears his own name — real or sample?), Shame rows, payout split, is "Jake" a real person, real `payment_handle` (currently the FAKE "Venmo @gfy-duck", publicly displayed), calcutta↔Field reconciliation, 2 missing handicaps. |

## Everything else

| # | Item | Status | Owner | Notes |
|---|------|--------|-------|-------|
| 1 | **Pre-links runway (README "Live scoring")** | `blocked` | RILEY | Re-paste BOTH .gs on the live sheet + redeploy endpoint (Edit → New version), Info keys score_endpoint/form_url, run polish(), then the drill (now 8 steps: SC-PROBE first, concurrent leg 5b, idem-key purge in cleanup). Captain links DO NOT go out before this passes. Same work as R2. |
| 2 | **Real-phone scorer eyeball at 390w under production Jost** | `verify` | RILEY | S11 harness ran fallback Futura; one live phone look at the card/pad closes the font caveat (D1 fix measured, not eyeballed on-device). |
| 3 | **Sheet doc-id sits in public git history since f261fd2** | `decision` | RILEY | Recommendation: accept-residual (sheet 401s anonymous; drill re-verifies sharing). Rotate the sheet if declined. |
| 4 | **§23 copy-integrity bundle** | `done` | YOURS | Merged 2026-08-01 (6c0bd09), DEPLOYED with b166fa5 on 08-24. |
| 5 | **Accepted a11y follow-ups** | `done` | YOURS | Built 2026-08-31 (a11y-wave 0d0aa39): sheet focus/Tab-trap/Escape/focus-return, board-btn instant on confirm + season-pin check, suppressed-board on-surface explainer. |
| 6 | **Live sheet still carries template sample data — THE DATA PACKAGE (R3)** | `decision` | RILEY | Riley ruled 09-02: "get rid of the fake people and start from scratch." Clean-slate dry-run passed 50/50; site empty-states proven honest; Seed refuses safely on an empty Field. CLEAR: Field all rows (incl. the 2027 Duck test row), Scores 3, Calcutta 3, Rooms 4, Ledger 1, Pairings 2, Schedule 3, Champions 2025/2024/2019, Shame 2. **CONFIRM BEFORE DELETE:** Champions 2026 row ("Jake, Riley, Dave, Gerald" — not flagged as template residue, looks real), Shame rows (Moose/Sully), payout 50/30/20 keep-or-replace, is "Jake" real. KEEP: Invites' 4 real rows, Course (verified real — see #16), Info keys (but `payment_handle` "Venmo @gfy-duck" is FAKE and public; 2027 dates/first_tee when planned). **Acceptance gate (§27 EV-ACCEPT): after the fill `npm run event-ready` must be GREEN or every surviving FAIL explained per row — a residue FAIL that survives real data is a validation bug wearing a residue costume.** Live count 2026-09-24: 25 actionable residue rows (info 10, rooms 4, schedule 3, payout 3, shame 2, champions 2, calcutta 1). |
| 7 | **§24 player-information wave** | `done` | YOURS | Announcements tab + unseen banner, event-phase Now/Next Home, C-FALLBACK honest fallbacks, tools/event-ready.mjs preflight. DEPLOYED 2026-08-24 (b166fa5); announce tab created + gid wired 08-31 (e30159e). |
| 8 | **Offered, Riley to rule** | `decision` | RILEY | (a) printable one-pager/QR lodge poster — **BUILT since, in §26**; (b) post-event results export + Champions backfill helper — **CEO recommends specced fast-follow, NOT pre-event** (it feeds the since-history the #19.1 fix depends on next year); (c) **`?preflight=1` in-site readiness view — CEO recommends pressing this one hard: the checks already exist as a CLI, and on tournament morning Riley should be glancing at a phone, not opening a laptop at the lodge. See #24.** |
| 9 | **Wire the accept→Field + invites workflow on the live sheet** | `open` | RILEY | Site half live since 08-24. SHEET half: add `committed` + `invited_by` headers on Invites row 1 → paste gfy-promote.gs (File → New → Script file, never over Code.gs) → re-paste sheet-polish.gs → run polish() → reload (GFY menu appears; if not, another onOpen may exist in the project). **Must come AFTER R1** — #16 and #19.1 both fix scripts that get pasted here, and pasting twice is strictly worse. Grounded 09-01: Invites header still 5-col; Riley is recording accepts as `status=Yes`, which nothing reads. |
| 10 | **§25a broadcast-finish wave** | `done` | YOURS | DEPLOYED 2026-08-28 (20c0bc0). |
| 11 | **§25a Riley eyeball items (live now)** | `open` | RILEY | (a) masthead bar full-bleed vs 1100px column seam at wide widths; (b) #mastCtx+#mastChip same text twice on Board; (c) chip shows live round inside an archive-year board (taste call); (d) phone pass: board arrows/glyphs/masthead at 390w. |
| 12 | **§25a deferred minors** | `open` | YOURS | roundNorm "1.0"→"10" quirk (unreachable via the real write path); renderHomeBoard second model pass per paint; T6 parser duplication. |
| 13 | **§25b + §26 build** | `done` | YOURS | §25b DEPLOYED 09-01 (becf0a1). §26 merged 69b3fc1, awaiting R1. |
| 14 | **Funnel name-list right-align defect** | `done` | YOURS | .mn-net (money, right-aligned) was borrowed by every funnel/rooms name list since v2.2. Fixed by the .name-list split (ef7d720). |
| 15 | **§25b deferred (test-tightness + one dress)** | `open` | YOURS | (a) T2b rm-block containment + T1a position:relative regex tightening; (b) .entry-name.blank italic placeholder dress. |
| 16 | **sheet-polish CF anchoring bug** | `done` | YOURS | **FIXED 2026-09-24 (f77985b).** `colorField_`/`colorRooms_` built CF formulas with un-anchored column refs; Sheets evaluates a CF formula relative to every cell of its range, so the ref shifted per column and the row tint only *appeared* to work because blanks coerce to FALSE (live Field row 7: B7 white, rest red). All three now `$`-anchored; `colorInvites_` already was. Smoke P8 detects any un-anchored ref and carries an anchor-stripped mutant so it cannot go vacuous. Proven RED pre-fix. **Landed before the #9 paste, as required.** |
| 17 | **Invites-flow hardening (rides with the #9 paste)** | `decision` | RILEY | From the 09-01 pressure test: (a) `status=Yes` recurrence — the gesture Riley already makes is still silently eaten after wiring; **recommend ABSORB (promote treats yes/y as committed) + validation help text**; (b) promote writes `year` raw → `parseInt`; (c) Seed prompt must NAME its candidates; (d) START HERE renders captain links + form dropdown from SAMPLE teams off-season — label or suppress; (e) `orderTabs_` leaves Rooms active → end on START HERE; (f) year-scope the red unpaid tint (archive rows red forever = wallpaper). |
| 18 | **Sheet legibility wave ("speaks across tabs")** | `open` | YOURS | `setTabColor` by role, header `setNote` on non-obvious columns, validation help text; auto-promote via installable `onCommittedEdit` (mobile has NO custom menus) gated on a 60s real-phone test; site: committed-but-Field-less person falls into "needs an invite" while on the owing list. **F-NAMES trap: START HERE is PUBLISHED — any cross-tab dashboard there is counts-only, never names.** |
| 19 | **#9 data decisions surfaced by the dry-run** | `partial` | RILEY | (1) **RESOLVED 2026-09-24 (efa762e):** with Field wiped every promoted player looked like a first-timer → whole field badged ROOKIE. Promote now only claims rookie-ness when Field carries a season EARLIER than the one promoted into; otherwise `since` is BLANK + "(since unknown — fill it)" and Riley hand-types the real one. First behavioural test of any promote rule (P9), proven RED pre-fix. (2) Field 2027 Duck row: resolved by the wipe. (3) `invited_by` blank for all four 2027 names → event-ready WARNs until sponsors recorded — **still Riley's**. (4) Do NOT run "Seed next season's Invites" until #6. (5) The Apps Script project also holds stale `triggers.gs` and the §18 spike `Untitled.gs`; that is R2's lane. |
| 20 | **§26 event kit** | `open` | RILEY | Merged 69b3fc1, suite green: `#tv` lodge mode, vendored QR encoder (decode-verified), print poster + captain cards. Ships with R1. |
| 21 | **§26 physical proof steps — BINDING before any real print run** | `open` | RILEY | **§27 K-SCAN-BIND pre-commits this now, because printing feels like "done" and this is the step fatigue eats at 11pm.** (a) **PHONE-CAMERA SCAN both QR codes on a PHYSICALLY PRINTED proof at print size** — the encoder is a from-memory reconstruction, decode-round-trip tested but never read by a camera, and quiet-zone/margin/error-correction failures appear only on paper; (b) 2027 Info dates/course/lodging fill; (c) print vendor + paper; (d) TV hardware + on-lodge `#tv` eyeball; (e) **arm scoring first** — the cards promise "Scores post to the live board". |
| 22 | **§26 deferred** | `open` | YOURS | activeSeason vs scorerSeason bare-year divergence; SITE_ROOT canonicalization; lodge announcements on `#tv` (hidden by ruling); copy line for Riley's list: "No events scheduled today." |
| 23 | **§27 go-live hardening** | `done` | YOURS | **Built 2026-09-24 (8a17499).** SC-IDENT identity envelope on `doGet` (both exit paths); SC-PROBE `npm run check-endpoint` classifying the DEPLOYMENT not the HTTP call — **verified against the live deployment, correctly returns STUB**; drill step 1 = the probe, sweep pass condition pinned to the row appearing, new concurrent leg 5b (first leg that actually exercises the DocumentLock), cleanup purges `idem:drill:` keys; OPS-RESTORE three-line mid-event restore runbook. Spec §27. |
| 24 | **`?preflight=1` phone readiness view — CEO's one "genuinely missing" capability** | `decision` | RILEY | The checks already exist (`npm run event-ready`); this surfaces them as a phone view so that on tournament morning Riley sees *scoring armed / N of M captains submitting / sheet healthy* at a glance instead of opening a laptop at a lodge. CEO: "combine it with a live scores-in counter and that single view IS the operational cockpit — highest day-of leverage of anything unspecced." **Deliberately NOT built: it is a new user surface and scope is Riley's.** Needs a yes + a brainstorm before any build. |
| 25 | **Post-event results export + Champions backfill** | `open` | YOURS | CEO ruling: real, but fast-follow — spec it, schedule it post-event, keep it off the pre-event critical path. Matters because this year's result feeds next year's Champions/`since` history, which is the same history #19.1's honest-rookie rule depends on. |
