# GFY — Backlog

> S13 format. Status: `open`|`decision`|`verify`|`blocked`|`partial`|`done`. Owner: **YOURS** (agent) | **RILEY**.
> Pipe-table with numbered bold-title rows — parses in BOTH mc-ground's needs-you grep and mc-unified's narrative panel.

| # | Item | Status | Owner | Notes |
|---|------|--------|-------|-------|
| 1 | **Pre-links runway (README "Live scoring")** | `blocked` | RILEY | Re-paste BOTH .gs on the live sheet + redeploy endpoint (Edit → New version), Info keys score_endpoint/form_url, run polish(), then the 25-min SC-DRILL (now incl. the third-number keep-then-reopen step). Captain links DO NOT go out before this passes. |
| 2 | **Real-phone scorer eyeball at 390w under production Jost** | `verify` | RILEY | S11 harness ran fallback Futura; one live phone look at the card/pad closes the font caveat (D1 fix measured, not eyeballed on-device). |
| 3 | **Sheet doc-id sits in public git history since f261fd2** | `decision` | RILEY | Recommendation: accept-residual (sheet 401s anonymous; drill re-verifies sharing). Rotate the sheet if declined. |
| 4 | **§23 copy-integrity bundle** | `done` | YOURS | Merged 2026-08-01 (6c0bd09), DEPLOYED with b166fa5 on 08-24. Stale "in flight" status corrected 08-28. |
| 5 | **Accepted follow-ups (post-tournament fine)** | `done` | YOURS | Built 2026-08-31 (a11y-wave 0d0aa39, 304/304): sheet focus/Tab-trap/Escape/focus-return (both sheets, shared pad wiring); board-btn instant on confirm + season-pin check; suppressed-board on-surface explainer (pinned copy). Hole-panel-head hardening was already carried by D3-era sgPanel checks (verified, not duplicated). |
| 6 | **Live sheet still carries template sample data** | `decision` | RILEY | Audit 2026-08-15: field/scores/calcutta/rooms are template sample rows (Duck/Hammer/Tex; sample cards edited in place) behind real-looking dates; schedule ends at Round One; no per-round tee times. ~~Real event = next year~~ **SUPERSEDED 09-02: Riley ruled "get rid of the fake people and start from scratch" — wipe now.** Proposed scope (clean-slate dry-run 50/50; site empty-states proven honest; Seed refuses safely on empty Field): CLEAR Field all rows (incl 2027 Duck test row — resolves #19.2), Scores 3, Calcutta 3, Rooms 4, Ledger 1, Pairings 2, Schedule 3, Champions 2025/2024/2019 sample rows, Shame 2. RILEY CONFIRMS before delete: Champions 2026 row ("Jake, Riley, Dave, Gerald" — NOT flagged as template residue, looks real), Shame rows (Moose/Sully), Payout 50/30/20 keep-or-replace, is "Jake" a real person. KEEP: Invites 4 real rows, Course (real 18), Info keys (but payment_handle "Venmo @gfy-duck" is FAKE and shown publicly — replace; 2027 dates/first_tee when planned). Replace with real data before links go out — §24's event-ready.mjs will police the verbatim residue mechanically. |
| 7 | **§24 player-information wave (spec rev 2, pressure-tested)** | `open` | YOURS | Announcements tab + unseen banner, event-phase Now/Next Home (reuses existing 60s loop + visibility seam), C-FALLBACK honest fallbacks, tools/event-ready.mjs preflight (9 checks incl. cross-tab + fallback parity). 4-lens pressure test 2026-08-15: ~30 findings triaged, 2 rev-1 defects were mine (phantom 3-min poller vs existing 60s loop; date-only parseDate vs watermark). **DEPLOYED 2026-08-24 (main @ b166fa5).** Remaining = RILEY: create `announce` tab in live sheet (re-run polish()) + hand over its gid → agent wires config.js GID.announce + push; then `npm run event-ready`. |
| 8 | **Offered, Riley to rule (from pressure-test scout)** | `decision` | RILEY | (a) printable one-pager/QR lodge poster (M, pull-only friendly); (b) post-event results export + Champions backfill helper (L, post-event); (c) ?preflight=1 in-site readiness view (M, operator phone-side). None folded into §24. |
| 9 | **Wire the accept→Field + invites workflow on the live sheet** | `open` | RILEY | **PUSHED LIVE 2026-08-24 (main @ b166fa5, Pages built, live md5 == tip).** Site half is live; the SHEET half is these steps: add `committed` + `invited_by` headers on Invites row 1 → paste gfy-promote.gs (File → New → Script file, never over Code.gs) → re-paste sheet-polish.gs (checkbox list + Invites CF + START HERE grew) → run polish() → reload sheet, GFY menu appears (if it doesn't: another onOpen may exist in the live script project). Ships: Promote committed → Field (NOCLOBBER, since carried, content-anchored append), Seed next season's Invites (veterans-first, out-for-good skipped), invited_by sponsor record (admin-only "via X"), orange/red Invites tints, Field count strip + veterans-first pre-draft sort. README "The invite list" has the full workflow. **Grounded 2026-09-01 (live CSV):** Invites header is still 5-col; Riley is recording accepts as `status=Yes` (Jeff, Riley — 2027), a value nothing reads (site ignores benign status; INVITES_STATUS vocab is declined/out only) — so two real yeses are invisible to the site and to Field. START HERE on the live sheet is the pre-migration polish() text (no Promote guidance). Wiring #9 is the fix; Riley opened a "make the sheet speak across tabs" design conversation same day. |
| 10 | **§25a broadcast-finish wave DEPLOYED 2026-08-28 (main @ 20c0bc0)** | `done` | RILEY | v2.1-invites @ merge aa96a44, suite 294/294: 5-token score colors + glyph rings + legend, mastheads + honest round chip, movement arrows (10-min basis, year+suppression guarded), event-phase Home top-5 slice (season-pinned), lower-third. 32-frame render close in worktree s25a workspace. Push word covers this + the invites waves. |
| 11 | **§25a Riley eyeball items (preview, post-push)** | `open` | RILEY | (a) masthead bar full-bleed vs 1100px column seam at wide widths; (b) #mastCtx+#mastChip same text twice on Board; (c) chip shows live round inside an archive-year board (T3 M6 — taste call, parked); (d) phone pass: board arrows/glyphs/masthead at 390w (battery says clean; eyes still owed). |
| 12 | **§25a deferred minors (final-review triage: fine-to-defer)** | `open` | YOURS | roundNorm hand-typed "1.0"→"10" quirk (pre-existing, unreachable via real write path); renderHomeBoard second model pass per paint (O(n) tiny); T6 parser duplication; S25a names outside groupTally letters. |
| 13 | **§25b finish-polish + §26 event kit QUEUED** | `open` | YOURS | §25b = B-FIN (tabular-nums sweep, photo frames, motion). §26 = #tv lodge mode, tools/print poster+captain cards, vendored QR. Build after Riley's §25a preview look. |
| 14 | **Funnel name-list right-align defect (found 08-27 live S11)** | `done` | YOURS | .mn-net (money-column, right-aligned) borrowed by all funnel/rooms name lists since v2.2 — names orphaned at right edge. Fixed by .name-list split (polish wave ef7d720) + honest calendar CTA + phase-aware board eyebrow + draft labels in the same wave. |
| 14 | **§25b finish-polish MERGED (awaiting push)** | `open` | RILEY | v2.1-invites @ 5513064, suite 315/315: numeral/rule/gold audits, empty-state ceremony (4 classes), photo/map frames, score-flash + arrow entrance (honesty-gated, reduced-motion clean). 20-frame render close all PASS. |
| 15 | **§25b deferred (test-tightness + one dress)** | `open` | YOURS | (a) T2b rm-block containment + T1a position:relative regex tightening (render battery compensates meanwhile); (b) .entry-name.blank last italic placeholder dress — next polish window. |
| 16 | **sheet-polish CF anchoring bug — CONFIRMED LIVE 2026-09-01** | `open` | YOURS | `colorField_`/`colorRooms_` formulas use un-anchored column refs (`G2=FALSE`, `D2=$A2`, `D2<>""`); Sheets CF shifts them per column, so the "row" tints only hold because blank cells coerce to FALSE — visible on live Field row 7 (2027 Duck): B7 white, rest red (paid_date non-blank). Rookie gold effectively column-A-only; Rooms orange year-cell-only. Fix = `$`-anchor all three (Invites rules already anchored); add smoke P8 assert; harness B6 must require `$`. MUST land before the #9 polish paste (pasting twice is strictly worse). Found by 3-reviewer pressure test (mechanics lens). |
| 17 | **Invites-flow hardening wave (rides with #9 paste — Riley decisions)** | `decision` | RILEY | From the 09-01 pressure test (3 reviewers, ~30 findings): (a) `status=Yes` recurrence — the gesture Riley already made is still silently eaten after wiring; options: ABSORB (promote treats status yes/y as committed + ticks the box; CF orange + event-ready in lockstep) vs REJECT/HELP (validation help text "declined / out only — a YES goes in the committed box"); recommend absorb+help text. (b) promote writes `year` raw → `parseInt` (text-year would break the since==year tint). (c) Seed prompt must NAME its candidates (dry-run: on live data it would seed the 5 template names). (d) START HERE renders CAPTAIN SCORING LINKS + form dropdown from SAMPLE teams off-season → label "last season's — rebuilt after the draft" or suppress. (e) `orderTabs_` leaves Rooms active → end on START HERE. (f) year-scope the red unpaid tint (archive rows red forever = wallpaper) — taste call. |
| 18 | **Sheet legibility wave ("speaks across tabs") — follow-up, spec first** | `open` | YOURS | Cheapest high-leverage answer to Riley's question 2, all mobile-visible + idempotent in polish(): `setTabColor` by role (edit-now green / derived grey / do-not-touch red for Form Responses 1), header `setNote` on non-obvious columns (since, team, status×2, committed, invited_by, paid_date, guest:), validation help text. Plus: auto-promote via installable `onCommittedEdit` (mobile has NO custom menus; same NOCLOBBER core; tint-clear as the signal) — gate on a 60-s real-phone test; site: committed-but-Field-only person falls into "needs an invite" while on the owing list (no committed stage in nextYearModel) — fix w/ tests; event-ready WARN for paid_date-without-deposit. **F-NAMES trap:** START HERE is PUBLISHED — any cross-tab dashboard there is counts-only, never names. |
| 19 | **#9 data decisions (Riley) surfaced by the dry-run** | `decision` | RILEY | (1) **AMPLIFIED by the 09-02 wipe ruling: with Field emptied, EVERY promoted player is a "first-timer" → since=promoted-year → whole field badges ROOKIE.** Fix wave MUST add: when Field has zero prior history, promote writes since BLANK + "(since unknown — fill it)" (S1 NULL-over-guess; harness I2 demonstrates the fabrication). Riley hand-types real since per player as they commit. (2) Field 2027 Duck: RESOLVED by the 09-02 wipe (test row — the 08:47-08:48 onDepositEdit runs were Riley exercising the trigger). (3) `invited_by` blank for all four 2027 names → event-ready WARNs until sponsors recorded. (4) Do NOT run "Seed next season's Invites" until #6. (5) Apps Script project also holds stale `triggers.gs` (=ba65dbf) + the §18 spike `Untitled.gs` (duplicate doPost/doGet; HEAD's /dev serves the spike) — that is #1's lane; the versioned web app (v1, Jul 30) is unaffected by #9. Rollback refs: live Code.gs/triggers.gs == `ba65dbf` byte-exact (sha verified). |
| 16 | **§26 event kit MERGED (awaiting push)** | `open` | RILEY | v2.1-invites @ 69b3fc1, suite 331/331: #tv lodge mode (3 panels, 20s rotation, honest stamps/stale/empty, §20 suppression parity), vendored QR encoder (decode-verified round-trips), tools/print make-kit (poster + captain cards, vault guard, per-card TZ stamps, [TBD]/[ 2027 DATES ] honesty). Fable 'Yes'. |
| 17 | **§26 Riley proof steps (BINDING before real printing)** | `open` | RILEY | (a) PHONE-SCAN both QR codes on a printed sample — the encoder is a from-memory reconstruction, decode-tested but never camera-scanned; (b) 2027 Info dates/course/lodging fill; (c) print vendor + paper; (d) TV hardware + on-lodge eyeball; (e) ARM SCORING before cards go out (cards say 'Scores post to the live board' — BACKLOG #1 sequences first). |
| 18 | **§26 deferred** | `open` | YOURS | activeSeason vs scorerSeason bare-year divergence (pre-existing site oddity); SITE_ROOT canonicalization into config.js (tools single-source suffices now); lodge announcements on TV (hidden — revisit if Riley wants them on screen); I5 crash-kill harness polish; new copy line for Riley's list: 'No events scheduled today.'. |

